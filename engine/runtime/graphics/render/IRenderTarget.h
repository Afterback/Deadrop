#pragma once

namespace deadrop
{
    namespace render
    {
        // an interface to expose the render target functionality
        class IRenderTarget
        {
        public:
            // virtual destructor
            virtual ~IRenderTarget() {};
        };
    }
}
